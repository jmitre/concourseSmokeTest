---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: resource-smoke-test

run:
  path: cd
  args: [resource-smoke-test]

run:
  path: ll

run:
  path: ./run.sh
